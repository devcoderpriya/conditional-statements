//to check number is odd using if statement
#include <stdio.h>
int main() {
    int a;
    printf("enter the number:");
    scanf("%d",&a);
    if(a%2!=0)
    {
        printf("number is odd");
    }

    return 0;
}
