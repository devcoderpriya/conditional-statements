#include<stdio.h>
int main()
{
    int a;
    printf("enter the 1st number \n :");
    scanf("%d",&a);
     int b;
    printf("enter the 2nd number \n :");
    scanf("%d",&b);
     int c;
    printf("enter the 2nd number \n :");
    scanf("%d",&c);
 (a>b && a>c) ? a:(b>c) ? printf("b is max"):printf("c is max");
     return 0;
}
