#include <stdio.h>
int main() {
    int num = 29, i = 2, isPrime = 1;
    while (i <= num / 2) {
        if (num % i == 0) {
            isPrime = 0;
            break;
        }
        i++;
    }
    if (isPrime) {
        printf("Prime\n");
    } else {
        printf("Not Prime\n");
    }
    return 0;
}
