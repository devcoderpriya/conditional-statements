include <stdio.h>

int main() {
    char sp;
    printf("Enter an alphabet: ");
    scanf(" %c", &sp);
    char ep;
    printf("Enter an alphabet: ");
    scanf(" %c", &ep);

   

    while (sp >= ep) {
        printf("%c ", sp);
        sp=sp-1;
    }

    return 0;
}
