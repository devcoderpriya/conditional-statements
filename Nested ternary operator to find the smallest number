#include <stdio.h>

int main() {
    int a = 15, b = 10, c = 20;

    
    int smallest = (a < b) ? ((a < c) ? a : c) : ((b < c) ? b : c);

    printf("The smallest number is: %d\n", smallest);

    return 0;
}
